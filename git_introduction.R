# send commands to the terminal
system(
  'git config --global user.email "z5164942@ad.unsw.edu.au";
   git config --global user.name "z5164942";'
)

# minimum info git needs to know to interact with Rstudio 
